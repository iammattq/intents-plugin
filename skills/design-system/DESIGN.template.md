# Design System

> Project-specific design conventions. Copy this file to your repo root as `DESIGN.md` and fill in each section with your actual choices. The `design-system` skill will auto-load it whenever UI files are touched.

## Component Library

<!-- What component primitives do you use? -->
<!-- Examples: shadcn/ui customized in components/ui/, Radix UI + custom wrappers, MUI v6, fully custom -->

- Library:
- Location of shared components:
- Customization approach:

## Design Tokens

<!-- Where are design tokens defined, and what's the usage policy? -->
<!-- Examples: CSS variables in app/globals.css via @theme; Tailwind v4 @theme directive; tokens.json with Style Dictionary -->

- Token definition location:
- Usage rule (e.g., "always reference tokens by name; never hardcode hex/rem/px"):
- Token naming convention:

## Composition Patterns

<!-- How do components compose? Atomic? Feature-based? Something else? -->

- Organization style:
- File/folder conventions:
- Props patterns (e.g., `asChild`, slot composition, compound components):

## Extension Policy

<!-- When do you customize an existing component vs. create a new primitive? -->

- When to extend an existing component:
- When to create a new primitive:
- Where custom components live:
- Review gate (if any) for new primitives:

## Accessibility Baseline

<!-- What's the minimum bar for every UI component? -->

- WCAG level target (A / AA / AAA):
- Focus indicator standard:
- Contrast ratio requirements:
- Motion handling (`prefers-reduced-motion`):
- Keyboard support expectations:

## Anti-Patterns

<!-- Things you've explicitly decided NOT to do in this project. Surprise-free rules. -->

-
-
-
